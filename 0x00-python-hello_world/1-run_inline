#!/bin/bash

# Check if PYCODE variable is set
if [ -z "$PYCODE" ]; then
echo "Error: PYCODE environment variable is not set"
exit 1
fi

# Run Python code stored in PYCODE variable
python3 -c "$PYCODE"

